{
	"name": "dollarpe",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"install:apps": "bun install --cwd apps/cli --no-save",
		"dev": "bun run dev:cli",
		"dev:cli": "bun run --cwd apps/cli dev",
		"build": "bun run build:cli",
		"build:cli": "bun run --cwd apps/cli build"
	}
}
